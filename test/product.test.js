const request = require('supertest')
const app = require('../app')
const { Product, sequelize } = require('../models')
const { queryInterface } = sequelize

describe('Product Routes', ()=>{
  
  describe('Test in create', ()=>{

    describe('test in name')

  })

})